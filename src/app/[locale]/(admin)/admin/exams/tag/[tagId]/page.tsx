"use server";
import React from "react";

const Page = async () =>
	// { params }: { params: { tagId: string } & Locale }
	{
		// const { data } = await GetData(`/tag/${params.tagId}`);

		return <>{/*<UpdateTag locale={params.locale} data={data} />*/}</>;
	};

export default Page;
