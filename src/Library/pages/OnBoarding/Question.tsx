import React from "react";
// import { useSelector } from "react-redux";
// import { QuestionShell, QuestionStyles } from "./Question.styles";

const Question = () => {
	// const [openImage, setOpenImage] = useState(false);
	// useEffect(() => {
	// 	document.addEventListener("keydown", function (e) {
	// 		e.key === "Escape" ? setOpenImage(false) : undefined;
	// 	});
	// }, []);

	// const { dirIsLTR } = useSelector((state: any) => state.ChangeDirectionAnswer);

	return (
		<>
			{/*<QuestionShell align={"center"} justify={"space-between"}>*/}
			{/*	<QuestionStyles dirIsLTR={dirIsLTR}>{question} </QuestionStyles>*/}
			{/*</QuestionShell>*/}
		</>
	);
};

export default Question;
