import React from "react";

const QuestionAndAnswer = () =>
	// 	{
	// 	question,
	// 	answers,
	// }: {
	// 	question: string;
	// 	answers: { body: string; isCorrect: boolean }[];
	// 	type: "multi" | "single" | "match";
	// }

	{
		// const { counter } = useSelector((state: any) => state.openAndClose);
		// // const [active, setActive] = React.useState<number | null>(null);
		// const answersSize = answers.length;
		// useEffect(() => {
		// 	document.addEventListener("keydown", function (e) {
		// 		const num = Number(e.key);
		// 		if (!Number.isNaN(num) && num > 0 && num <= answersSize) {
		// 			// const KeyDown: string = e.key;
		// 			const n = Number(e.key) - 1;
		// 			setActive(n);
		// 		}
		// 	});
		// }, []);
		return (
			<>
				{/*<QuestionAndAnswerShell seeAllQuestions={counter}>*/}
				{/*	<Question question={question} />*/}
				{/*</QuestionAndAnswerShell>*/}
			</>
		);
	};

export default QuestionAndAnswer;
