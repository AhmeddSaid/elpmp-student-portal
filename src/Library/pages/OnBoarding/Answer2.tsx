import React from "react";

const QAnswer = () =>
	// {
	// 	// question,
	// 	// answers,
	// 	// type,
	// }: {
	// 	question: string;
	// 	answers: { body: ReactNode; isCorrect: boolean }[]; // body as ReactNode
	// 	type: "multi" | "single" | "match";
	// },
	{
		// const { counter } = useSelector((state: any) => state.openAndClose);
		// const [active, setActive] = React.useState<number | null>(null);
		// const answersSize = answers.length;
		//
		// useEffect(() => {
		// 	document.addEventListener("keydown", function (e) {
		// 		const num = Number(e.key);
		// 		if (!Number.isNaN(num) && num > 0 && num <= answersSize) {
		// 			const n = num - 1;
		// 			setActive(n);
		// 		}
		// 	});
		// }, [answersSize]);

		return (
			<>
				{/*{answers.map((answer, index) => (*/}
				{/*	<li key={index} onClick={() => setActive(index)}>*/}
				{/*		<Answer*/}
				{/*			type={type}*/}
				{/*			index={index + 2}*/}
				{/*			body={answer.body} // Pass JSX directly*/}
				{/*			isCorrect={answer.isCorrect}*/}
				{/*		/>*/}
				{/*	</li>*/}
				{/*))}*/}
			</>
		);
	};

export default QAnswer;
