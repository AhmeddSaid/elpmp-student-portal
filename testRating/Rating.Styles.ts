"use client";

import styled from "styled-components";

export const RatingShell = styled.div`
	width: fit-content;

	& > div {
		text-align: start;
		width: fit-content;
		//transform: scale(0.5) translateX(50%);

		div {
			//width: 50px;
		}
	}
`;
